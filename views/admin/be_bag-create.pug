extends be_base

include ../../mixins/_formFields
include ../../mixins/_formSelectOptions

block content

	main.container
	
		section.product__item 		

			h2.admin__page--heading Add New Bag
			
			form.bag__form--create(method="POST" enctype="multipart/form-data")
				.product__form--group
					+formRows('name','text','')
				.product__form--group
					+formRows('description','text','')
				.product__form--group
					label.product__form--label(for="original-price") PRICE
					input#original-price.product__form--input(
						type="number"
						name="original-price"
					)
				.product__form--group
					+formSelectCodeNone('category', categories, null)
					+formSelect('color', ['black', 'white', 'red', 'blue', 'green', 'yellow', 'pink', 'purple', 'orange', 'grey', 'brown'], null)


				
				//-- Image Upload Section (Cover + Multiple Images) --//
				
				.product__form--img-box-new
					label.product__form--img-label(for='bag-image-cover') Upload Main Image (Cover):
					input#bag-image-cover(type='file', name='imageCover', accept='image/*')

				.product__form--img-box-new
					label.product__form--img-label Upload Additional Images:
					input#bag-image-array(type="file", accept="image/*", name="images", multiple)
					

				button.product__form--btn(type="submit") Confirm